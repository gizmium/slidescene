{
  "name": "journey",
  "version": "0.0.1",
  "scripts": {
    "start": "browser-sync start --files='index.html, css/**/*.css, js/**/*.js' --server"
  },
  "devDependencies": {
    "browser-sync": "^2.24.5",
    "browserify": "^16.2.2"
  },
  "private": true
}
